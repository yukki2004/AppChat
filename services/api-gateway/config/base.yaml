port: "8080"
log_level: "info"
core_service_url: "http://localhost:8090"
redis_host: "localhost"
redis_port: "6379"
redis_password: ""
cookie_domain: ""
# Extra CORS origins beyond the fixed localhost:5500 test-client ones — comma-separated, e.g.
# "http://192.168.1.20:5500" to test from a phone on the same LAN (see test-client/README.md).
# Empty by default; set via APP_CORS_EXTRA_ORIGINS, never hardcode a LAN IP in the yaml (it's
# different per dev machine/network).
cors_extra_origins: ""
